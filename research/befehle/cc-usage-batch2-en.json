[
  {
    "slug": "add-dir",
    "whenGood": [
      {"title": "Briefly access a second codebase without switching sessions", "example": "/add-dir ../shared-lib — look up a utility function from the shared lib while continuing work in the main project"},
      {"title": "Pull in reference files from a neighboring folder while keeping the prompt cache intact", "example": "/add-dir ../design-tokens — make design tokens available as reference for a styling task"}
    ],
    "whenBad": [
      {"title": "Wanting to permanently switch to another project with its own CLAUDE.md", "why": "/add-dir does not automatically load the .claude/ configuration of the added directory", "alternative": "use /cd instead — it picks up the target folder's CLAUDE.md and moves the session into its project storage"}
    ]
  },
  {
    "slug": "advisor",
    "whenGood": [
      {"title": "Bring in a stronger model for a second opinion before a risky architecture decision", "example": "/advisor opus — get a second opinion before implementing a database migration"},
      {"title": "Cut costs in a long loop by combining a cheap executor with an advisor consulted only when needed", "example": "/advisor sonnet — as advisor for an automation loop otherwise running on haiku"}
    ],
    "whenBad": [
      {"title": "Leaving it on for every trivial small task", "why": "each advisory round costs extra tokens/time with no benefit for routine work", "alternative": "open /advisor with no argument and turn it off, re-enable only at real decision points"}
    ]
  },
  {
    "slug": "agents",
    "whenGood": [
      {"title": "Ask Claude to create a new subagent configuration", "example": "/agents, then: \"create a subagent for database migrations for me\""},
      {"title": "Review existing subagent definitions before adjusting them in the current project", "example": "/agents to be pointed to .claude/agents/"}
    ],
    "whenBad": [
      {"title": "Expecting an interactive management UI", "why": "since v2.1.198, /agents only shows a hint instead of an interactive UI", "alternative": "edit .claude/agents/ or ~/.claude/agents/ directly in an editor"}
    ]
  },
  {
    "slug": "autofix-pr",
    "whenGood": [
      {"title": "Let an open PR run unattended against CI failures", "example": "/autofix-pr only fix lint and type errors — after pushing a PR, while working on something else"},
      {"title": "Have reviewer comments automatically turned into fixes", "example": "/autofix-pr address all review comments from the last review round"}
    ],
    "whenBad": [
      {"title": "Changes haven't been pushed as a PR yet", "why": "the command detects the PR via 'gh pr view' and needs Claude Code on the web access — with no open PR it finds nothing", "alternative": "commit/push normally and open a PR with gh pr create first, then run /autofix-pr"}
    ]
  },
  {
    "slug": "batch",
    "whenGood": [
      {"title": "A large change that splits into many independent units across the whole codebase", "example": "/batch rename the legacy HTTP client to the new SDK across all 15 services"},
      {"title": "A migration where each unit should get its own PR and be tested in parallel", "example": "/batch migrate every module from CommonJS to ESM"}
    ],
    "whenBad": [
      {"title": "A small task touching one or two files", "why": "the planning phase plus isolated worktrees and per-unit PRs are pure overhead for a small task", "alternative": "use a normal prompt or a single subagent instead of /batch"},
      {"title": "No git repository present", "why": "/batch requires a git repository for the isolated per-unit worktrees", "alternative": "run 'git init' first, or make the change manually without /batch"}
    ]
  },
  {
    "slug": "btw",
    "whenGood": [
      {"title": "Ask a quick side question without loading up the main context", "example": "/btw what does the --frozen-lockfile flag do? — while mid-way through a feature session"},
      {"title": "Clarify an unrelated question without branching the ongoing conversation", "example": "/btw is there a Windows equivalent for this bash command?"}
    ],
    "whenBad": [
      {"title": "The answer will be needed later in the main context", "why": "/btw does not add the question and answer to the main conversation", "alternative": "ask as a normal message in the main chat; for a completely new topic use /clear for a fresh start instead"}
    ]
  },
  {
    "slug": "cd",
    "whenGood": [
      {"title": "Permanently move to the next project whose CLAUDE.md should apply from now on", "example": "/cd ../other-project — continue the session fully in the new project"},
      {"title": "Store the session in the target folder's project storage so --resume finds it there", "example": "/cd ~/work/client-b before starting the next task"}
    ],
    "whenBad": [
      {"title": "Just briefly need a file from a neighboring folder while the main project stays active", "why": "/cd fully switches the session instead of just adding access", "alternative": "use /add-dir to access the extra directory without moving"}
    ]
  },
  {
    "slug": "chrome",
    "whenGood": [
      {"title": "Set up the Claude in Chrome extension before letting Claude take actions in the browser", "example": "/chrome — enable the extension/check permissions before a browser automation task"},
      {"title": "Review existing Chrome integration settings", "example": "/chrome to check which sites/permissions the extension currently has"}
    ],
    "whenBad": [
      {"title": "Just wanting to read or search a webpage, no browser control needed", "why": "/chrome only opens the Chrome extension's settings, not a search function", "alternative": "use /web or /web-search for pure reading/searching the web"}
    ]
  },
  {
    "slug": "claude-api",
    "whenGood": [
      {"title": "Load reference material before writing your own code against the Claude API", "example": "/claude-api — before building a custom streaming handler for the Messages API"},
      {"title": "Migrate existing API code to a newer model", "example": "/claude-api migrate — update model IDs and thinking configuration in existing code"}
    ],
    "whenBad": [
      {"title": "The question is only about operating the Claude Code CLI itself, not your own API code", "why": "/claude-api loads API reference material (Python/TS/Java/...), not CLI documentation", "alternative": "use the claude-code-guide agent or the official Claude Code docs for questions about the CLI itself"}
    ]
  },
  {
    "slug": "color",
    "whenGood": [
      {"title": "Make several parallel sessions visually distinguishable", "example": "/color blue in session A, /color green in session B"},
      {"title": "Quickly set a random color for the current session", "example": "/color with no argument"}
    ],
    "whenBad": [
      {"title": "Expecting the color to persist for all future sessions", "why": "/color only sets the color for the current session", "alternative": "set it again at the start of each session, or configure /statusline for a persistently visible distinguishing marker"}
    ]
  },
  {
    "slug": "config",
    "whenGood": [
      {"title": "Set a single known value directly via command", "example": "/config theme=dark or /config model=sonnet"},
      {"title": "Browse interactively through all settings when unsure what to change", "example": "/config with no argument opens the settings UI"}
    ],
    "whenBad": [
      {"title": "Expecting the interactive dialog to work in a non-interactive script", "why": "the interactive UI isn't suited for automated/non-interactive (-p) runs", "alternative": "use /config key=value directly — it also works non-interactive and via Remote Control"}
    ]
  },
  {
    "slug": "context",
    "whenGood": [
      {"title": "Check how much context is still free before continuing a long session", "example": "/context all for the detailed breakdown"},
      {"title": "Find the cause of unusually high context usage", "example": "/context to identify context-heavy tools or bloated memory"}
    ],
    "whenBad": [
      {"title": "Actually wanting to free up context", "why": "/context only displays usage, it doesn't change the context itself", "alternative": "use /compact to summarize the conversation and free up context"}
    ]
  },
  {
    "slug": "copy",
    "whenGood": [
      {"title": "Grab the last response directly into the clipboard", "example": "/copy after a response containing a finished code snippet"},
      {"title": "Write the response to a file instead, over SSH without a working clipboard", "example": "/copy, then press 'w' to export to a file"}
    ],
    "whenBad": [
      {"title": "Wanting to copy an older response, not the last one", "why": "without a number argument, /copy always copies only the last response", "alternative": "use /copy 3 for the third-to-last response, or /export for the whole conversation"}
    ]
  },
  {
    "slug": "cost",
    "whenGood": [
      {"title": "Quickly check API cost/token usage without remembering the name /usage", "example": "/cost"}
    ],
    "whenBad": [
      {"title": "Expecting extra functionality beyond /usage", "why": "/cost is just an alias, not a separate command with different behavior", "alternative": "use /usage directly, or /stats for the stats tab"}
    ]
  },
  {
    "slug": "dataviz",
    "whenGood": [
      {"title": "Get the right chart form and a colorblind-safe palette before building a dashboard", "example": "/dataviz before building a KPI dashboard with several chart types"},
      {"title": "Have an existing visualization's color choices checked for contrast/colorblind safety", "example": "/dataviz to validate the color mapping of an existing chart"}
    ],
    "whenBad": [
      {"title": "Pure data analysis with no visualization to follow", "why": "/dataviz helps with chart design, not with the data analysis itself", "alternative": "use a normal analysis prompt, bring in /dataviz only once results are being shown as a chart"}
    ]
  },
  {
    "slug": "debug",
    "whenGood": [
      {"title": "Start capturing debug logs from now on for a reproducible problem", "example": "/debug MCP server keeps disconnecting — right before triggering the problem again"},
      {"title": "Systematically enable logging before a hard-to-trace bug hunt", "example": "/debug to capture tool calls and errors in the session debug log"}
    ],
    "whenBad": [
      {"title": "The error already happened earlier", "why": "/debug only captures logs from the moment it's activated, earlier events are missing", "alternative": "restart the session with 'claude --debug' to log from the very start"}
    ]
  },
  {
    "slug": "deep-research",
    "whenGood": [
      {"title": "A thorough, multi-source research task with citations on an open question", "example": "/deep-research what are current best practices for LLM prompt caching in production?"},
      {"title": "Let research run in the background while working on something else", "example": "/deep-research compare pricing models of major vector database providers"}
    ],
    "whenBad": [
      {"title": "A quick single lookup of a concrete fact", "why": "fanning out across many subagents is unnecessary overhead for one quick lookup", "alternative": "use /web-search for the quick single lookup"}
    ]
  },
  {
    "slug": "design-login",
    "whenGood": [
      {"title": "One-time authorization before first using /design-sync", "example": "/design-login before the first sync of your own React design system"}
    ],
    "whenBad": [
      {"title": "Expecting the command to already sync designs", "why": "/design-login only authorizes access via the claude.ai account, it doesn't sync anything itself", "alternative": "run /design-sync afterwards for the actual sync"}
    ]
  },
  {
    "slug": "design-sync",
    "whenGood": [
      {"title": "Upload your own React component system so generated designs use real project components", "example": "set up /design-sync once after /design-login"},
      {"title": "Re-sync after larger component changes", "example": "run /design-sync again after reworking the button and card component set"}
    ],
    "whenBad": [
      {"title": "Using it on Bedrock/Vertex/Foundry", "why": "/design-sync is not available on these platforms", "alternative": "use a standard Anthropic account/Claude Code access, or work with generic components without design-sync"},
      {"title": "Starting the first sync right before a deadline", "why": "the first sync verifies every component and can take hours on large repos", "alternative": "schedule it well ahead of time instead of right before a deadline"}
    ]
  },
  {
    "slug": "desktop",
    "whenGood": [
      {"title": "Seamlessly carry a running terminal session over to the desktop app", "example": "/desktop mid-session, to keep working on a bigger screen"},
      {"title": "Switch from CLI to a GUI-centered workflow without losing context", "example": "/desktop when moving from a laptop on the go to a desktop machine"}
    ],
    "whenBad": [
      {"title": "Linux without a Claude subscription", "why": "/desktop requires macOS or Windows plus a Claude subscription", "alternative": "stay in the terminal session, or use /remote-control to control the session from claude.ai instead"}
    ]
  },
  {
    "slug": "diff",
    "whenGood": [
      {"title": "Go through uncommitted changes file by file before committing", "example": "/diff after several edits, to review each changed file"},
      {"title": "Trace which change originated in which conversation turn", "example": "/diff, then switch between git diff and Claude turns with left/right"}
    ],
    "whenBad": [
      {"title": "Wanting to compare changes across multiple commits/branches", "why": "/diff only shows uncommitted changes and turn-by-turn diffs, not commit history", "alternative": "use a normal 'git diff <ref1> <ref2>' in the terminal for cross-commit comparisons"}
    ]
  },
  {
    "slug": "exit",
    "whenGood": [
      {"title": "Cleanly end the session once the task is done", "example": "/exit after finishing the work"}
    ],
    "whenBad": [
      {"title": "Actually wanting to stop an attached background session", "why": "in an attached background session, /exit only disconnects — the session keeps running", "alternative": "manage/stop the background session via 'claude agents' instead of just /exit"}
    ]
  },
  {
    "slug": "export",
    "whenGood": [
      {"title": "Save or share the whole conversation as text", "example": "/export session-notes.txt"},
      {"title": "Decide between clipboard and file via the dialog", "example": "/export with no argument opens the choice"}
    ],
    "whenBad": [
      {"title": "Only wanting to copy the last response", "why": "/export exports the entire conversation, not a single response", "alternative": "use /copy for just the last (or, with a number, the Nth-last) response"}
    ]
  },
  {
    "slug": "fast",
    "whenGood": [
      {"title": "Prioritize speed over depth for a series of simple, repetitive edits", "example": "/fast for a run of similar, simple changes"},
      {"title": "Turn fast mode back off once more complex tasks come up", "example": "call /fast again to toggle it back"}
    ],
    "whenBad": [
      {"title": "Non-interactive use without fastMode in the start settings", "why": "non-interactive (-p) toggling of /fast only works in sessions started with fastMode set in --settings", "alternative": "pass fastMode directly in --settings when starting the session"}
    ]
  },
  {
    "slug": "feedback",
    "whenGood": [
      {"title": "Report a bug in Claude Code itself", "example": "/feedback or /bug after hitting a reproducible tool error"},
      {"title": "Share a conversation with someone", "example": "/share to pass along a helpful conversation"}
    ],
    "whenBad": [
      {"title": "Wanting to report a bug in your own project code", "why": "/feedback addresses issues with Claude Code itself, not your own application", "alternative": "use a normal bug-fix prompt or file an issue in your own repo"}
    ]
  },
  {
    "slug": "fewer-permission-prompts",
    "whenGood": [
      {"title": "Auto-allow recurring, harmless read-only commands after a few sessions", "example": "/fewer-permission-prompts after several sessions full of repeated permission prompts for git status, ls, etc."},
      {"title": "Generate a sensible allowlist baseline for team onboarding", "example": "/fewer-permission-prompts to pre-populate .claude/settings.json with common read-only calls"}
    ],
    "whenBad": [
      {"title": "Wanting to blanket-allow write/risky commands too, without prompts", "why": "the skill specifically scans for common read-only Bash/MCP calls, not write commands", "alternative": "keep allowing write/risky commands manually and deliberately in .claude/settings.json (e.g. via the update-config skill)"}
    ]
  }
]
