[
  {
    "slug": "focus",
    "whenGood": [
      {"title": "Get an overview of long multi-tool turns", "example": "Before a task with many file edits, turn on /focus to see only the last prompt, a one-line tool-call summary with diffstats, and the final response instead of every intermediate message."},
      {"title": "Screen-sharing or demoing without tool noise", "example": "Enable /focus before a presentation so viewers see only the key steps instead of every single tool call."}
    ],
    "whenBad": [
      {"title": "Debugging a failed tool call", "why": "Focus collapses tool calls into one-liners — details like a stack trace or the full diff content are hidden.", "alternative": "Run /focus again to turn it off and see the full turn output."}
    ]
  },
  {
    "slug": "heapdump",
    "whenGood": [
      {"title": "Claude Code is using unusually high memory", "example": "The terminal gets noticeably slow and Task Manager shows high RAM usage → run /heapdump; the snapshot lands on ~/Desktop (or the home directory on Linux without a Desktop folder) for analysis."},
      {"title": "Document memory usage for a bug report", "example": "Before reporting a suspected memory leak, run /heapdump and attach the generated snapshot file to the bug report."}
    ],
    "whenBad": [
      {"title": "The conversation's context window is full", "why": "heapdump measures the process's JavaScript memory usage, not the conversation's token usage.", "alternative": "Use /context to visualize context usage as a grid."},
      {"title": "General installation or setup problems", "why": "heapdump only produces a memory snapshot, not a diagnosis of installation or configuration issues.", "alternative": "Use /doctor for a setup check."}
    ]
  },
  {
    "slug": "help",
    "whenGood": [
      {"title": "Getting first orientation in Claude Code", "example": "Right after installing, type /help to see the list of available commands."},
      {"title": "Looking up a forgotten command name", "example": "You only vaguely remember a command name → /help shows the full overview to look it up."}
    ],
    "whenBad": [
      {"title": "Clarifying detailed questions about one specific feature", "why": "/help only gives a short overview of commands, not a detailed explanation of individual features.", "alternative": "Consult the official docs (code.claude.com/docs) or ask directly."}
    ]
  },
  {
    "slug": "hooks",
    "whenGood": [
      {"title": "Check which hooks currently apply to tool events", "example": "Before adding a new pre-tool hook, run /hooks first to avoid conflicts with existing hook configurations."},
      {"title": "Figure out why a tool call was altered or blocked", "example": "After a Bash call gets unexpectedly denied, open /hooks to see whether an existing hook configuration is responsible."}
    ],
    "whenBad": [
      {"title": "Editing or reconfiguring hook rules", "why": "/hooks only displays the configurations, it is not an editing interface.", "alternative": "Edit the hook configuration directly in settings.json, or use the update-config skill."}
    ]
  },
  {
    "slug": "ide",
    "whenGood": [
      {"title": "Verify the IDE connection", "example": "After opening VS Code alongside the terminal, run /ide to confirm Claude Code is connected."},
      {"title": "Check the status of multiple open IDE integrations", "example": "With several editor windows open, use /ide to see which integration is currently active."}
    ],
    "whenBad": [
      {"title": "Installing the IDE extension for the first time", "why": "/ide only manages existing integrations and shows their status — it does not install a new editor extension.", "alternative": "Install the matching Claude Code extension directly from the editor's marketplace (e.g. the VS Code Marketplace)."}
    ]
  },
  {
    "slug": "init",
    "whenGood": [
      {"title": "Prepare a new repository for Claude Code", "example": "In a freshly cloned project, run /init so Claude creates an initial CLAUDE.md with project context."},
      {"title": "Full setup including skills, hooks, and memory", "example": "With the environment variable CLAUDE_CODE_NEW_INIT=1 set, start /init to interactively set up skills, hooks, and personal memory files as well."}
    ],
    "whenBad": [
      {"title": "A well-maintained, project-specific CLAUDE.md already exists", "why": "Running /init again can overwrite or needlessly alter existing, carefully curated project docs.", "alternative": "Edit CLAUDE.md manually and selectively instead of re-initializing the project."}
    ]
  },
  {
    "slug": "insights",
    "whenGood": [
      {"title": "Reflect on your own Claude Code usage", "example": "After an intense project week, run /insights to see which project areas got the most work."},
      {"title": "Spot recurring friction points in your workflow", "example": "Use /insights to surface patterns like frequent clarifying questions or interaction issues across sessions."}
    ],
    "whenBad": [
      {"title": "Getting an instant short summary of the current session", "why": "/insights analyzes sessions after the fact for patterns and friction points, it doesn't give a quick one-sentence summary of the ongoing conversation.", "alternative": "Use /recap for a one-line summary of the current session."}
    ]
  },
  {
    "slug": "install-github-app",
    "whenGood": [
      {"title": "Set up automated PR checks or CI integration", "example": "Run /install-github-app in a team repository to enable Claude for automated PR workflows via GitHub Actions."},
      {"title": "Prepare repo access for later Claude automations", "example": "For a new project, walk through /install-github-app including repo selection and secrets so workflows like /autofix-pr can work."}
    ],
    "whenBad": [
      {"title": "Working purely locally, without GitHub integration", "why": "/install-github-app sets up a connection to GitHub including optional Actions and secrets — unnecessary overhead if PRs or CI will never run through GitHub.", "alternative": "Keep working locally with Git and skip the GitHub app."}
    ]
  },
  {
    "slug": "install-slack-app",
    "whenGood": [
      {"title": "Make Claude usable in a team Slack workspace", "example": "Run /install-slack-app to enable Claude Tag in the company workspace for colleagues."},
      {"title": "Start requests directly from a Slack channel", "example": "After installation, mention Claude in a Slack channel to kick off a request directly from chat."}
    ],
    "whenBad": [
      {"title": "No Slack in use, or no admin rights in the workspace", "why": "Installation requires an OAuth flow in the browser and appropriate workspace permissions — without Slack usage it provides no benefit.", "alternative": "Use Claude Code directly in the terminal or IDE."}
    ]
  },
  {
    "slug": "keybindings",
    "whenGood": [
      {"title": "Customize keyboard shortcuts", "example": "Open /keybindings to change a shortcut that conflicts with another application."}
    ],
    "whenBad": [
      {"title": "Just quickly checking which shortcuts exist, without changing anything", "why": "/keybindings opens the editable file directly rather than a plain overview.", "alternative": "Consult the keyboard shortcuts documentation instead of opening the file."}
    ]
  },
  {
    "slug": "login",
    "whenGood": [
      {"title": "First-time sign-in or switching accounts", "example": "On a new machine, run /login to connect with the Anthropic account."}
    ],
    "whenBad": [
      {"title": "Just checking whether you're already logged in", "why": "/login starts the sign-in flow directly, it doesn't show a plain status view.", "alternative": "Use /status to see account and connection info without starting a new login flow."}
    ]
  },
  {
    "slug": "logout",
    "whenGood": [
      {"title": "Cleanly disconnect an account from a device", "example": "On a shared machine, run /logout after finishing work to sign out of your own Anthropic account."}
    ],
    "whenBad": [
      {"title": "Just briefly checking whether, and with which account, you're logged in", "why": "/logout ends the session immediately instead of just displaying information.", "alternative": "Use /status to view account info without signing out."}
    ]
  },
  {
    "slug": "mcp",
    "whenGood": [
      {"title": "Fix the connection status of an MCP server", "example": "An MCP server stops responding → run /mcp reconnect <server> to rebuild the connection."},
      {"title": "Selectively disable individual MCP servers", "example": "Use /mcp disable <server> to temporarily turn off a rarely used server and reduce context/tool footprint."}
    ],
    "whenBad": [
      {"title": "Setting up a brand-new MCP server for the first time", "why": "/mcp only manages existing connections (reconnect, enable, disable) and OAuth authentication, it does not create a new server configuration.", "alternative": "Add the server to the MCP configuration (project or user level) first, then use /mcp to manage it."}
    ]
  },
  {
    "slug": "memory",
    "whenGood": [
      {"title": "Capture a permanent rule for all future sessions", "example": "Use /memory to write a standing instruction like 'always use TypeScript strict mode' into CLAUDE.md instead of repeating it every time."},
      {"title": "Review auto-memory entries", "example": "Run /memory to see which rules Claude has automatically saved as auto-memory from earlier sessions."}
    ],
    "whenBad": [
      {"title": "An instruction only applies to the current task", "why": "/memory writes to CLAUDE.md, which persists beyond the session — unnecessary for a one-off instruction and it clutters the file.", "alternative": "Give the instruction directly in the current session's prompt without saving it to CLAUDE.md."}
    ]
  },
  {
    "slug": "mobile",
    "whenGood": [
      {"title": "Install the Claude mobile app", "example": "Run /mobile to scan the QR code and download the app to your phone."}
    ],
    "whenBad": [
      {"title": "Continuing to steer a running terminal session on the go, without reinstalling the app", "why": "/mobile only shows a QR code for installing the app, it doesn't hand off a running session.", "alternative": "Enable /remote-control to steer the session remotely from claude.ai or the app."}
    ]
  },
  {
    "slug": "passes",
    "whenGood": [
      {"title": "Share a free week of Claude Code with friends", "example": "Run /passes (if the account is eligible) to give away a free week."}
    ],
    "whenBad": [
      {"title": "Managing your own subscription or checking account status", "why": "/passes is only for sharing free weeks and only visible if the account is eligible — it has nothing to do with managing your own plan.", "alternative": "Use /upgrade or /status to view your own subscription or account status."}
    ]
  },
  {
    "slug": "permissions",
    "whenGood": [
      {"title": "Add a targeted allow or deny rule for a tool", "example": "Open /permissions to add a deny rule for a specific Bash command in the interactive dialog."},
      {"title": "Review recently denied auto mode actions", "example": "Run /permissions to see which tool calls were recently denied in auto mode, then adjust a rule accordingly."}
    ],
    "whenBad": [
      {"title": "Bundling many recurring read-only prompts automatically", "why": "/permissions edits rules one at a time through an interactive dialog — cumbersome for an allowlist generated automatically from the transcript.", "alternative": "Use /fewer-permission-prompts, which builds a prioritized allowlist from your own transcripts."}
    ]
  },
  {
    "slug": "plugin",
    "whenGood": [
      {"title": "Install a new plugin directly", "example": "Run /plugin install <name> to add a plugin without going through the menu."},
      {"title": "See an overview of all installed plugins", "example": "Run /plugin with no argument to see in the plugin menu which plugins are active or inactive."}
    ],
    "whenBad": [
      {"title": "Just reloading already-active plugins after a file change", "why": "/plugin is for management (listing, installing, enabling), not for reloading already-active plugins after changes.", "alternative": "Use /reload-plugins to apply changes without restarting."}
    ]
  },
  {
    "slug": "powerup",
    "whenGood": [
      {"title": "Discover new Claude Code features playfully", "example": "After an update, start /powerup to discover new features through short interactive lessons with demos."},
      {"title": "Onboard newcomers to Claude Code features", "example": "Have a new team member go through /powerup instead of reading the entire documentation."}
    ],
    "whenBad": [
      {"title": "Looking up a specific, already-known feature", "why": "/powerup shows curated lessons, not a searchable reference of individual commands.", "alternative": "Use /help for the command overview, or consult the docs directly."}
    ]
  },
  {
    "slug": "privacy-settings",
    "whenGood": [
      {"title": "Check your own privacy settings", "example": "Open /privacy-settings to see which privacy options are currently set."},
      {"title": "Adjust privacy settings to match internal requirements", "example": "As a Pro or Max user, go through /privacy-settings to change settings to fit internal policy."}
    ],
    "whenBad": [
      {"title": "Free-plan account without a Pro or Max subscription", "why": "According to the docs, /privacy-settings is only visible to Pro and Max plan subscribers.", "alternative": "Upgrade to Pro/Max first (/upgrade), or check general account settings via /status."}
    ]
  },
  {
    "slug": "radio",
    "whenGood": [
      {"title": "Listen to lo-fi music in the background while coding", "example": "Run /radio to open 'Claude FM' in the browser."}
    ],
    "whenBad": [
      {"title": "Working on Bedrock, Vertex, or Foundry", "why": "According to the docs, /radio is not available on these platforms.", "alternative": "Use a separate music app or player alongside Claude Code."}
    ]
  },
  {
    "slug": "recap",
    "whenGood": [
      {"title": "Quickly get back up to speed after a break", "example": "After a pause, run /recap to see in one sentence where the session currently stands."},
      {"title": "Share a short status update", "example": "Use /recap to get a one-sentence summary of the current session state to pass along to colleagues."}
    ],
    "whenBad": [
      {"title": "Saving or sharing the full conversation history", "why": "/recap only gives a one-line summary, not the full text of the conversation.", "alternative": "Use /export to export the entire conversation as plain text."}
    ]
  },
  {
    "slug": "release-notes",
    "whenGood": [
      {"title": "Find out what changed after an update", "example": "Run /release-notes to read the changes in the most recently installed version."},
      {"title": "Look up the behavior of a specific version", "example": "Call /release-notes with a specific version number to check whether a fix is included in exactly that version."}
    ],
    "whenBad": [
      {"title": "Actually updating to the latest version", "why": "/release-notes only displays the changelog, it doesn't perform an update itself.", "alternative": "Use /upgrade to actually update."}
    ]
  },
  {
    "slug": "reload-plugins",
    "whenGood": [
      {"title": "Test a change to your own plugin immediately", "example": "After editing a plugin file, run /reload-plugins to apply the change without restarting the session."},
      {"title": "Catch load errors after a plugin change", "example": "Use /reload-plugins to immediately get a report on whether a newly edited plugin loads without errors."}
    ],
    "whenBad": [
      {"title": "Forcing a reload immediately despite a pending prompt-cache invalidation", "why": "/reload-plugins skips the reload by default when it would invalidate the prompt cache.", "alternative": "Use /reload-plugins --force to force the reload anyway."},
      {"title": "Only a single changed skill needs updating, not a plugin", "why": "/reload-plugins reloads plugins, not project-local skills or commands.", "alternative": "Use /reload-skills to re-scan skill and command directories."}
    ]
  },
  {
    "slug": "reload-skills",
    "whenGood": [
      {"title": "Make a newly added skill available during the running session", "example": "Add a skill to .claude/skills/ and then run /reload-skills instead of restarting the session."},
      {"title": "Verify a skill change was picked up correctly", "example": "After editing a SKILL.md, run /reload-skills and check the report of added or removed skills."}
    ],
    "whenBad": [
      {"title": "An entire plugin (not just loose skills/commands) was changed", "why": "/reload-skills scans skill and command directories, not the plugin structure including loaded MCP tools.", "alternative": "Use /reload-plugins to reload the entire plugin."}
    ]
  },
  {
    "slug": "remote-control",
    "whenGood": [
      {"title": "Steer a local terminal session from a phone or the web", "example": "Enable /remote-control, then steer the running terminal session from claude.ai, e.g. while away from your desk."},
      {"title": "Keep an eye on a running task from another device", "example": "Turn on /remote-control before starting a longer task, so progress can be checked later from a different device."}
    ],
    "whenBad": [
      {"title": "Bringing a web session into the local terminal, not the other way around", "why": "/remote-control makes the local session controllable from claude.ai — it's the wrong command for the opposite direction.", "alternative": "Use /teleport to bring a web session into the terminal."},
      {"title": "Simply continuing work in the desktop app on another device", "why": "/remote-control makes the session remotely controllable but doesn't move it into a desktop app.", "alternative": "Use /desktop to continue the session in the Claude Code desktop app (requires macOS or Windows plus a Claude subscription)."}
    ]
  }
]
